python-pptx>=0.6.23
Pillow>=10.0.0
imageio-ffmpeg>=0.4.9
# Optional: neural TTS via Microsoft Edge (free, no API key). Used when
# `npm run video -- --narrate --engine edge` is selected.
edge-tts>=6.1.0
