{
  "name": "quest-demo-agent",
  "version": "0.1.0",
  "private": true,
  "description": "AI Demo Generator for Quest Data Modeler — Phase 1 (Playwright flow runner + screenshot capture).",
  "scripts": {
    "build": "tsc",
    "demo": "ts-node agent/runner.ts",
    "bootstrap": "ts-node scripts/bootstrap.ts",
    "recon": "ts-node scripts/recon.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "playwright": "^1.47.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18"
  }
}
